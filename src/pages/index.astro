---
import { getCollection } from "astro:content";
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import { sortMDByDate } from "@/utils";
import profile from "../../public/profile.jpeg";
import { Image } from "astro:assets";

const MAX_POSTS = 10;
const allPosts = await getCollection("post");
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title">Jayden Harrod</h1>
		<h2 class="text">Product & Engineering Leader</h2>
		<div class="mb-4 flex gap-12">
			<Image class="h-20 w-20 rounded-2xl" src={profile} alt="Profile Image" />
			<p class="text">
				When I was a young teenager, I was lucky enough to get my hands on Adobe Creative Suite (in
				DVD form!). I dove headfirst, spending all my time mastering Photoshop, After Effects, and
				Illustrator through countless tutorials.
			</p>
		</div>
		<div>
			<p class="text">
				That early passion for design and technology has led me down an incredibly rewarding path,
				where I've been able to build a versatile career spanning Product, Design, and Engineering
				for multiple award winning Australian startups.
			</p>
			<p class="text">
				Today, I get to use all that experience in my role as a fractional CTO via my agency for
				purpose, <a class="cactus-link" href="https://stewardship.ventures">Stewardship Ventures</a>
				🚀 - I'm really excited about the work we're doing for startups!
			</p>
		</div>

		<!-- <section aria-label="Blog post list">
			<h2 class="title mb-4 text-xl">Latest Posts</h2>
			<ul class="space-y-4 sm:space-y-2">
				{
					allPostsByDate.map((p) => (
						<li class="flex flex-col gap-x-2 sm:flex-row">
							<PostPreview post={p} />
						</li>
					))
				}
			</ul>
		</section> -->
	</section>
</PageLayout>
